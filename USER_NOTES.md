# User Notes

- Use MCP tools (`mcp_polytope_exec`, `mcp_polytope_get-container-logs`, `mcp_polytope_list-containers`) to inspect containers and run commands inside them. Do NOT use `docker exec` directly — use `mcp_polytope_exec` instead.
